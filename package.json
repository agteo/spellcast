{
  "name": "spellcast",
  "version": "0.9.0",
  "description": "Gesture-unlocked visual effects on LiteRT.js browser motion capture",
  "type": "module",
  "scripts": {
    "setup-wasm": "node scripts/copy-wasm.mjs",
    "predev": "npm run setup-wasm",
    "prebuild": "npm run setup-wasm",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@litertjs/core": "^2.5.2",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
